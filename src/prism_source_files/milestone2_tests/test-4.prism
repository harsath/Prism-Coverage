FUNCTION INT for_loop(INT value1) {
	INT returner = value1;
	
	FOR (INT i = 1; i < 5; i = i + 1;) {
		returner = returner * 3;
	}
	
	FOR (INT a = 1; a < 5; a = a + 1;) {
		returner = returner - 2;
	}
	
	IF ( returner > 100 ) {
		RETURN POW(returner, 2);
	} ELSE {
		IF (returner < 90) {
			RETURN MAX(returner, 90);
		} ELSE {
			RETURN returner;
		}
	}
}

FUNCTION INT main() {
	INT return_value = for_loop(1);

	// expected: 81
	RETURN return_value;
}
