FUNCTION INT foo(INT a1, INT a2) {
       IF (a1 <= a2){
       		RETURN a1 + a2;
       }
       ELSE {
       		RETURN a1 * a2;
       }
}

FUNCTION INT main() {
	INT a = 15;
	INT b = 15;
	a = foo(a, b);
	RETURN a;
}